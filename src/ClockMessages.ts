import { MessageType } from "@coderatparadise/showrunner-network";

export const MessageClockCue: MessageType = "clock:cue";

export const MessageClockUncue: MessageType = "clock:uncue";

export const MessageClockPlay: MessageType = "clock:play";

export const MessageClockPause: MessageType = "clock:pause";

export const MessageClockStop: MessageType = "clock:stop";

export const MessageClockRecue: MessageType = "clock:recue";

export const MessageClockSetTime: MessageType = "clock:settime";

export const MessageClockData: MessageType = "clock:data";

export const MessageClockCurrent: MessageType = "clock:current";

export const MessageClockConfig: MessageType = "clock:config";

export const MessageClockUpdateConfig: MessageType = "clock:updateconifg";

export const MessageClockList: MessageType = "clock:list";

export const MessageClockCreate: MessageType = "clock:create";

export const MessageClockRemove: MessageType = "clock:remove";

export const MessageClockCommand: MessageType = "clock:command";

export const MessageClockChapter: MessageType = "clock:chapter";

export const MessageClockAddChapter: MessageType = "clock:addchapter";

export const MessageClockRemoveChapter: MessageType = "clock:removechapter";
